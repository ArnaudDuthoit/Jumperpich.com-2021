<div class="filt">

    <div class="a-filter p-4 d-flex justify-content-center">

        {{ form_start(form, {'attr': {'class' : 'js-filter-form'}}) }}

        <div class="spinner-border js-loading" role="status" aria-hidden="true" style="display: none">
            <div class="sr-only">Loading ... </div>
        </div>

        {{ form_widget(form.Tags) }}

        <button type="submit" class="btn btn__custom"> Filtrer </button>

        {{ form_end(form) }}

    </div>

</div>
